get:
  tags:
    - common
  description: |
    通知を取得
  responses:
    '200':
      description: |
        通知情報
      content:
        text/plain:
          schema:
            $ref: "../definition/get_notify.yaml"
