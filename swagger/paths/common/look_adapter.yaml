get:
  tags:
    - common
  description: |
    接続確認を開始する
  responses:
    '200':
      description: |
        接続確認を開始
      content:
        text/plain:
          schema:
            $ref: "../../schema/success.yaml"
