get:
  tags:
    - common
  description: |
    Wi-Fi の接続を情報を取得する
  responses:
    "200":
      description: |
        Wi-Fi の接続を情報
      content:
        text/plain:
          schema:
            $ref: "../../schema/get_wifi_setting.yaml"