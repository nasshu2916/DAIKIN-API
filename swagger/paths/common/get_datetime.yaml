get:
  tags:
    - common
  summary: 機器の時刻を取得
  responses:
    '200':
      description: |
        機器の時刻
      content:
        text/plain:
          schema:
            $ref: "../../schema/get_datetime.yaml"
