get:
  tags:
    - common
  description: |
    機器の基本情報を取得
  responses:
    '200':
      description: |
        機器の基本情報
      content:
        text/plain:
          schema:
            $ref: "../../schema/basic_info.yaml"
