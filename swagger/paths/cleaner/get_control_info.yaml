get:
  tags:
    - cleaner
  description: |
    空気清浄機のモードを取得
  responses:
    "200":
      description: |
        空気清浄機のモードを取得
      content:
        text/plain:
          schema:
            $ref: "../../schema/get_control_info.yaml"