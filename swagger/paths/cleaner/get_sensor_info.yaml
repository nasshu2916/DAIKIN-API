get:
  tags:
    - cleaner
  summary: 空気清浄機のセンサの情報を取得
  responses:
    "200":
      description: |
        空気清浄機のセンサの情報を取得
      content:
        text/plain:
          schema:
            $ref: "../../schema/get_sensor_info.yaml"